import React from 'react'
import { Container, Header, Icon } from 'semantic-ui-react'
import "./WeHaveAnEmptyProject.css";


const WeHaveAnEmptyProject = () => (
    <div>
        <Header textAlign="center">
            This project is currently empty. But this component is loading which means that react is working. Also, if it centers, it means that semantic is working too.
        </ Header>
    </div>
  )

export default WeHaveAnEmptyProject